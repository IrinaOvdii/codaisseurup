<%= form_for [@event, @event.registrations.new] do |f| %>
  <p><strong>Price:</strong> <span>€</span> <%= @event.price %></p>

  <hr>

  <div class="form-group">
    <label>How many guests: </label>
    <div class="input-group">
      <div class="input-group-addon"></div>
      <%= f.number_field :guest_count, placeholder: "How many people do you want to register?(max 3!)", class: "form-control" %>
    </div>
  </div>


  <div class="form-group">
    <%= f.check_box :status %> Status
  </div>

  <div class="actions">
    <%= f.submit "Register Now", class: "btn btn-success" %>
  </div>
<% end %>
